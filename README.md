# embedded-alerts-libs

Reusable embedding-provider, rule-evaluation, event, delivery, and policy libraries.

Initialized through `DEN-1949` as a testable `libs` foundation. Product behavior continues through focused pull requests.

```bash
python3 scripts/verify_repo.py
```
